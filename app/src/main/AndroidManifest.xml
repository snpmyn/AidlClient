<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zsp.aidlclient">

    <!--Android 11+ (API 30+) 客户端必须声明 queries 才能访问其它 APP-->
    <!--否则系统直接拦截 (Android 11 包可见性安全机制)-->
    <queries>
        <package android:name="com.zsp.aidlserver" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AidlClient"
        tools:ignore="AllowBackup">
        <activity android:name=".book.BookActivity" />
        <activity android:name=".calculate.CalculateActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>